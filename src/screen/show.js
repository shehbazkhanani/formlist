import { useLocation } from "react-router-dom"

function Show() {
    const location = useLocation()
    const data = location.state.data;
    return (
        <>
       
        </>
        )
}

export default Show